# JSON Schemas

This section contains the JSON schemas for the Kinetic Trust Protocol.

!!! info "Draft schemas — contributors welcome"
    These schemas are in active draft. If you spot gaps or have suggestions, please open an issue or PR (see [Contributing](../community/contributing.md)) to collaborate on the structure and fields.

## Available Schemas

??? note "Context Tensor (context-tensor.json)"
    ```json
    --8<-- "docs/schemas/context-tensor.json"
    ```

??? note "Sensor Configuration (sensor-config.json)"
    ```json
    --8<-- "docs/schemas/sensor-config.json"
    ```

??? note "Soul Constraint (soul-constraint.json)"
    ```json
    --8<-- "docs/schemas/soul-constraint.json"
    ```

??? note "Trust Proof (trust-proof.json)"
    ```json
    --8<-- "docs/schemas/trust-proof.json"
    ```

## Usage

These schemas define the data structures used throughout the KTP implementation. They can be used for validation, code generation, and documentation purposes.
